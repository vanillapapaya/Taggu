# Web
fastapi
uvicorn[standard]
jinja2

# ML (로컬 모드용 — API 모드만 쓸 거면 생략 가능)
torch
open-clip-torch
transformers
accelerate
qwen-vl-utils
# 4bit 양자화 (7B 모델을 ~6GB VRAM에서 돌릴 때 필요)
bitsandbytes

# AI Provider SDK (각 provider는 lazy import — 안 깔려있어도 다른 provider는 동작)
openai
anthropic
google-genai

# WD14 (캐릭터 태깅) + CCIP (캐릭터 학습 임베딩) — 둘 다 ONNX 추론
onnxruntime
huggingface_hub

# Image
Pillow

# Core
numpy
