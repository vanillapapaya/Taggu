# Web
fastapi
uvicorn[standard]
jinja2

# ML (로컬 모드용 — API 모드만 쓸 거면 생략 가능)
torch
open-clip-torch
transformers
accelerate
qwen-vl-utils
# 4bit 양자화 (7B 모델을 ~6GB VRAM에서 돌릴 때 필요)
bitsandbytes

# AI Provider SDK (각 provider는 lazy import — 안 깔려있어도 다른 provider는 동작)
openai
anthropic
google-genai

# WD14 (캐릭터 태깅) + CCIP (캐릭터 학습 임베딩) — 둘 다 ONNX 추론
onnxruntime
huggingface_hub

# Image
Pillow

# 유튜브 짤 생성 (유튜브 구간 → GIF/JPEG)
#  - yt-dlp: 영상 구간 다운로드. 유튜브 변동에 따라 가끔 'uv pip install -U yt-dlp' 필요
#  - imageio-ffmpeg: ffmpeg 바이너리를 venv 안에 동봉 (winget 없이 자동 설치)
yt-dlp
imageio-ffmpeg

# Core
numpy
